<?xml version="1.0"?>
<ruleset name="Xigen">
    <description>The Xigen PHP coding standard.</description>

    <!-- There should not be any code in the bundle Resources directory. -->
    <exclude-pattern>*/Resources/*</exclude-pattern>

    <rule ref="PSR1"/>
    <rule ref="PSR2"/>
    <rule ref="PEAR"/>
    <rule ref="Generic"/>
    <rule ref="Squiz"/>
</ruleset>